/// Database implementation for initializing, updating, and reading from
/// database tables.

use stanza;

use mysql::prelude;

pub struct Database;

impl Database {
    pub fn init() -> Database {
        Database
    }

}


